version: "3.8"

services:
  api-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: meerkat-be:dev
    ports:
      - "11111:11111"
    volumes:
      - .:/app
      - /app/venv
    environment:
      - ENV=development

